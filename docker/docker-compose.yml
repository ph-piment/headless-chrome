version: '3.7'

networks:
  backend:
    driver: bridge

services:
  headless-browser:
    container_name: headless-browser
    build: ./headless-browser
    networks:
      - backend

  workspace:
    container_name: workspace
    build: ./go
    tty: true
    volumes:
      - ../code/go:/go/src/work
    depends_on:
      - headless-browser
    networks:
      - backend
